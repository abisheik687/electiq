#!/bin/sh
REPO_SIZE=$(git count-objects -vH | grep 'size-pack' | awk '{print $2}')
SIZE_MB=$(du -sm . --exclude=.git | cut -f1)
if [ "$SIZE_MB" -gt 9 ]; then
  echo "ERROR: Repo size is ${SIZE_MB}MB — exceeds 9MB limit. Remove large files before pushing."
  exit 1
fi
